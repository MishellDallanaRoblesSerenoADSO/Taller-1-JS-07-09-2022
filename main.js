addEventListener("DOMContentLoaded",()=>{
    // 8.Escribir un programa que evalúe la siguiente expresión:
    // (a+7*c)/(b+2-a)+2*b 
    let a=Number(prompt("coloque el valor de a: "))
    let b=Number(prompt("coloque el valor de b: "))
    let c=Number(prompt("coloque el valor de c: "))

    let ecuacion= Number(prompt("Ecuacion (a+7*c)/(b+2-a)+2*b", (a+7*c)/(b+2-a) +2*b))
    console.log(`El valor de la ecuacion es: "${ecuacion}"`)
})